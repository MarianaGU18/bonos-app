# Puerto
server.port=8080

# MySQL (Docker)
spring.datasource.url=jdbc:mysql://mysql:3306/bonos_db
spring.datasource.username=bonos_user
spring.datasource.password=bonos_pass

# JWT
jwt.secret=CAMBIA_ESTO_POR_UN_SECRET_LARGO_Y_SEGURO
jwt.expiration=86400000

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

spring.application.name=backend

banxico.token=${BANXICO_TOKEN}